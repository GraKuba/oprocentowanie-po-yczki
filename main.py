initial_loan = 12000
fixed_instalment = 200
intrest_rate = 3

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * initial_loan - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)

inflation = float(input())
debt = (1 + (inflation + intrest_rate) / 1200) * debt - fixed_instalment
print("Zadluzenie =", debt)


